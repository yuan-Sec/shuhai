---
name: 书海
description: 安静、专注、可靠的本地电子书阅读体验
colors:
  background: "#101513"
  surface: "#171e1b"
  surfaceRaised: "#202925"
  text: "#f3f6f4"
  textMuted: "#aab7b1"
  accent: "#79b8a4"
  accentStrong: "#2f6f5e"
  border: "#35413c"
  danger: "#d97070"
typography:
  body: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
  reading: Georgia, "Noto Serif SC", serif
rounded:
  sm: 6px
  md: 10px
  lg: 14px
spacing:
  1: 8px
  2: 16px
  3: 24px
  4: 32px
components:
  button-primary: 强调色实底、深色文字、最小触控高度 44px
  button-secondary: 表面色实底、单像素边框、最小触控高度 44px
  nav-active: 低对比度强调色表面与清晰轮廓
  input: 深色表面、可见边框、明确的 focus-visible 焦点环
---

# Design System: 书海

## Overview

创意北极星是“静谧书桌”：界面像一张整理有序、光线柔和的阅读桌面，工具触手可及，但不会盖过书本。视觉应安静、专注、可靠，以 Foliate 和 Readest 一类成熟阅读器的清晰信息架构为参考，同时保持 Android 触控场景中的直接性。默认采用深色模式，核心内容容器保持克制宽度，移动端使用固定底部导航。

## Colors

深色主题以墨绿黑背景、深灰绿表面和鼠尾草绿强调色构成。背景承担大面积空间，表面色只用于结构分区和可交互控件，不把每一项内容都做成悬浮卡片。强调色用于主要操作、选中状态、进度和焦点；危险色仅用于清空等不可逆操作。浅色、护眼和米色主题保持相同语义关系。正文和关键控件至少满足 WCAG 2.2 AA 对比度，状态不能只依赖颜色表达。

## Typography

应用界面使用系统无衬线字体，避免额外字体下载并保持原生、快速和离线。页面标题使用 24px 左右的紧凑粗体，区块标题使用 16–18px，辅助信息不小于 12px。阅读正文可以选择系统字体或衬线字体，默认 18px、1.8 行距，并允许用户调整。数字统计使用等宽数字特性以减少跳动。

## Elevation

层级主要由背景色、边框和留白建立，默认不使用厚重阴影。顶栏、底部导航和抽屉可使用细边框与非常轻的结构阴影来标示边界。圆角以 6px、10px、14px 三档为限；按钮和筛选标签不使用夸张胶囊造型。动效限于 120–180ms 的状态反馈，启用“减少动态效果”后立即关闭非必要过渡。

## Components

主要按钮使用强调色实底，文字保持高对比度，并提供图标和文本双重提示。次要按钮使用表面色与边框。搜索框、排序框和视图切换器组成统一工具栏，在窄屏下自然换行。书籍卡片以封面、标题、作者、进度和快捷操作为核心，网格与列表共享同一信息顺序。统计页使用目标环、简洁数字块和低噪声柱状图。设置页按主题、阅读偏好、数据管理和关于分区。所有交互控件至少 44px，键盘焦点必须清晰可见。

## Do's and Don'ts

应优先呈现继续阅读、当前进度和常用动作；用结构留白而非装饰分隔信息；清楚说明本地存储、备份范围与失败状态；在小屏幕上保证搜索、筛选和导航可单手操作。不要使用仿真木质书架、纸张纹理或拟物翻页；不要使用霓虹渐变、玻璃拟态、复杂动画和无意义的发光效果；不要把所有内容放进相同的悬浮卡片；不要为了“高级感”降低文字对比度、缩小点击区域或隐藏标准操作。
